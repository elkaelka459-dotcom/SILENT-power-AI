export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/generate'&&request.method==='POST'){try{const b=await request.json(),topic=String(b.topic||'').trim();if(!topic)return Response.json({error:'Topic is required'},{status:400});if(!env.AI)return Response.json({error:'Workers AI binding is missing'},{status:500});const prompt=`You are Silent Power AI, an elite short-form motivational writer. Create ORIGINAL content only. Do not imitate or reproduce any named creator, existing video, copyrighted script, or recognizable person's words or voice. Topic: ${topic}. Language: ${b.language||'English'}. Duration: ${b.duration||45} seconds. Format: ${b.format||'Motivational podcast'}. Voice: ${b.tone||'Deep calm American male'}. Extra: ${b.extra||''}. Return ONLY valid JSON: {"script":"...","scenes":[{"n":1,"text":"...","shot":"CLOSE-UP|MEDIUM SHOT|PUNCH-IN","visual":"original cartoon scene description"}]}. Rules: strong hook first sentence; natural spoken language; 6-8 scenes; memorable ending; Silent Power CTA; no emojis; no quotes from sources.`;const r=await env.AI.run('@cf/meta/llama-3.1-8b-instruct',{messages:[{role:'system',content:'Output strict JSON only.'},{role:'user',content:prompt}],temperature:.8,max_tokens:1200});let text=(r?.response||'').replace(/^```json\s*/i,'').replace(/```\s*$/,'').trim();return Response.json(JSON.parse(text),{headers:{'cache-control':'no-store'}})}catch(e){return Response.json({error:'Generation error',detail:String(e?.message||e)},{status:500})}}return env.ASSETS.fetch(request)}};
